name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    name: build+test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      # This repo path-depends on ../ce-rs and ../ce (ce-cap + ce-identity live in ce-net/ce).
      # Check this repo into ./ce-expose and the deps as siblings so the relative paths resolve.
      - name: Checkout ce-expose
        uses: actions/checkout@v4
        with:
          path: ce-expose

      - name: Checkout ce-rs (path dep)
        uses: actions/checkout@v4
        with:
          repository: ce-net/ce-rs
          path: ce-rs

      - name: Checkout ce (ce-cap + ce-identity path deps)
        uses: actions/checkout@v4
        with:
          repository: ce-net/ce
          path: ce

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ce-expose

      - name: Build
        working-directory: ce-expose
        run: cargo build --verbose

      - name: Test
        working-directory: ce-expose
        run: cargo test --verbose
