# Optional: point the explorer at a specific node base URL in a production build.
# In `npm run dev` this is unnecessary — Vite proxies /ce-node to 127.0.0.1:8844.
# VITE_CE_NODE_URL=http://127.0.0.1:8844
