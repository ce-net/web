{
  "name": "ce-svelte",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vite + Svelte + TypeScript starter on CE — a collaborative counter persisted to ce.db with live updates over a room",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.1",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^4.2.18",
    "svelte-check": "^3.8.4",
    "tslib": "^2.6.3",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
