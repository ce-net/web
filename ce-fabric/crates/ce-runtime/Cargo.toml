[package]
name = "ce-runtime"
version = "0.1.0"
edition = "2024"
description = "CE execution-runtime seam: run a Workload independent of the backend (Docker, WASM, ...)."

[dependencies]
anyhow = "1"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
